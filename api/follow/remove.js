import { q } from '../_utils/db-node.js';
import { requireUser } from '../_utils/auth-node.js';
export default async function handler(req,res){ if(req.method!=='POST') return res.status(405).end(); try{ const me = requireUser(req); const { target } = req.body || {}; const { rows:a } = await q('select id from users where username=$1',[me]); const { rows:b } = await q('select id from users where username=$1',[target]); if(!a[0] || !b[0]) return res.status(404).json({ ok:false, error:'用户不存在' }); await q('delete from follows where follower_id=$1 and following_id=$2',[a[0].id, b[0].id]); res.json({ ok:true }); } catch(e){ const code = e.message==='missing token'?401:500; res.status(code).json({ ok:false, error:e.message }); } }
